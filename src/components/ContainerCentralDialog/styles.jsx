import styled from 'styled-components';

export const SessionOffering = styled.section`
  z-index: 1;
  max-width: 50%;
  border-radius: 5px;
  text-align: center;
  align-self: center;
`;

export const ArticleOffering = styled.article`
  align-self: center;
`;