import React from 'react'

import { ContainerMain } from './styles';

export default function ContainerPresentationLogin({ children }) {
  return (
    <ContainerMain>
      {children}
    </ContainerMain>
  );
}
